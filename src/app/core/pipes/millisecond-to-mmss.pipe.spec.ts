import { MilisecondToMMSSPipe } from './millisecond-to-mmss.pipe';

describe('MilisecondToMMSSPipe', () => {
  it('create an instance', () => {
    const pipe = new MilisecondToMMSSPipe();
    expect(pipe).toBeTruthy();
  });
});
